import plugin from 'tailwindcss/plugin'

// TODO: once the licensed FrostUI package is installed, replace this file's contents with:
//   export { default } from '@frostui/tailwindcss'
// and flip USE_TEMPLATE_PLUGIN to true in ../../../../tailwind.config.ts.
//
// This placeholder is a structurally valid, no-op Tailwind plugin so the build never
// breaks while USE_TEMPLATE_PLUGIN is false (which it is by default).
export default plugin(() => {})
